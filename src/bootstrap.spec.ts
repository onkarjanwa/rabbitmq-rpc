import chai from 'chai';
import chaiHttp from 'chai-http';
import mocha from 'mocha';

chai.use(chaiHttp);

mocha.before(() => {
});

mocha.beforeEach(() => {
});

mocha.afterEach(() => {
});

mocha.after(() => {
});
